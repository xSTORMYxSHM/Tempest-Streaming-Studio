{
  "name": "@tempest/contracts",
  "version": "0.21.0",
  "private": true,
  "description": "Versioned Tempest Streaming Studio API and asset contracts.",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "files": ["dist", "schemas"],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "node --test test/*.test.cjs"
  }
}
